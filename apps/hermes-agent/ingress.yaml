apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hermes-agent-ingress
  namespace: ai
  annotations:
    kubernetes.io/ingress.class: "traefik"
    traefik.ingress.kubernetes.io/router.middlewares: auth-authelia-forwardauth@kubernetescrd
spec:
  ingressClassName: traefik
  rules:
  - host: hermes.home.romangzz.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: hermes-agent-service
            port:
              number: 8081

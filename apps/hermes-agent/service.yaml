apiVersion: v1
kind: Service
metadata:
  name: hermes-agent-service
  namespace: ai
spec:
  type: ClusterIP
  ports:
  - port: 8081
    targetPort: 8642
    name: api
  selector:
    app: hermes-agent
